<div>
<p>投稿ユーザー画像：<%= image_tag @book.user.get_profile_image(100,100) %></p>

<h2>Title</h2>
<p><%= @book.title %></p>

<h2>Opinion</h2>
<p><%= @book.body %></p>

<%=link_to "Edit",edit_user_path(@book.id)%>
<%=link_to "Back",books_path%>


<% if @book.user == current_user %>
    <%= link_to "編集", edit_book_path(@book), method: :delete %>
    <%= link_to "削除", books_path(@book.id), method: :delete, "data-confirm" => "本当に削除しますか？" %>
  <% end %>
  </div>